import { useEffect } from 'react';

// Hook was used to initialize Lenis smooth scrolling. 
// It has been disabled to make scrolling static as requested.
export const useSmoothScroll = () => {
  useEffect(() => {
    // Smooth scrolling disabled
  }, []);
};
