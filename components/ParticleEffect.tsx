import React from 'react';

// This component is now a simple placeholder - similar to vitap.ac.in approach
// which doesn't use particle effects with the video
const ParticleEffect: React.FC = () => {
  // Return null as we're not using particles, keeping the component
  // to avoid breaking references elsewhere in the code
  return null;
};

export default ParticleEffect;
